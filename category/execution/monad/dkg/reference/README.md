# Reference DKG contract

This Solidity contract is a temporary executable specification for reviewing
and testing the native DKG contract in the parent directory.

Production execution uses only the native C++ contract. `monad-bft` owns its
ABI declarations and does not import Solidity sources or generated artifacts
from this directory. Remove this reference after native parity review is
complete.
