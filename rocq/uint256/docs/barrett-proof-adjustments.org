#+TITLE: Barrett Proof Adjustments Log
#+DATE: 2026-04-30

* Purpose

This note records theorem-strengthening and model changes made while proving
=rocq/uint256/theories/BarrettProofs.v=.  The rule for this phase is that
proof statements may be strengthened only when the added assumptions correspond
to C++ semantics or to documented semantic preconditions, not merely to make a
proof easier.

* Theorem-strengthening log

** Add admissible =BarrettParams= assumptions to constructor and wrapper theorems

The C++ type =barrett::reciprocal<Params>= has a template =requires= clause:

- =uint256_t{Params.min_denominator} > 0=
- =uint256_t{Params.min_denominator} <= uint256_t{Params.max_denominator}=
- =Params.input_bits > 0=

The Rocq model has a total =BarrettParams= record, so the corresponding
theorems must carry this admissibility as an explicit hypothesis.  Without it,
=constructor_sound_no_multiplier= is false.

Counterexample before strengthening:

- =min_denominator = 0=
- =max_denominator = 0=
- =input_bits = 1=
- =multiplier_bits = 0=
- =d = 0=

Then =valid_denominator params d = true=, because =valid_denominator= is only
the C++ runtime range check.  However, the constructed reciprocal stores a zero
denominator, while =reciprocal_invariant= requires =0 < denominator_Z rec=.

The proof file therefore introduces =params_admissible= as the semantic Rocq
form of the C++ template-level precondition and adds it to the constructor and
wrapper theorem statements that construct a reciprocal from =params= and =d=.

** Add multiplier storage-fit assumption to the multiplier constructor theorem

The C++ multiplier constructor stores =y= in a fixed
=words_t<MULTIPLIER_WORDS>= field and then asserts that the higher words of the
original =uint256_t= are zero.  The original Rocq theorem claimed
=multiplier_Z rec = to_Z_uint256 y= for every =y= whenever
=MULTIPLIER_WORDS > 0=, which is stronger than the C++ storage semantics.

Counterexample before strengthening:

- =multiplier_bits = 1=, so =MULTIPLIER_WORDS = 1=
- =y = 2^64=

The Rocq model, like the C++ object layout, stores only the low one-word
prefix of =y= in =multiplier_=.  The stored multiplier is zero, while
=to_Z_uint256 y = 2^64=.  The theorem therefore needs the semantic version of
the C++ debug assertion: =y= must fit in =MULTIPLIER_WORDS= words.

** Restrict multiplier constructor theorems to the reachable =BIT_SHIFT = 0= branch

The existing semantic-discrepancies log records that the generic C++
=numerator(y)= branch for =MULTIPLIER_WORDS > 0= and =BIT_SHIFT <> 0= appears
to read one word past the end of =y=.  The exported constant-multiplier
families instantiate =INPUT_BITS = 256=, so they use the reachable
=BIT_SHIFT = 0= path.

The Rocq model describes the intended shifted-word behavior for the dormant
branch, but proving that generic branch would not be a proof of the current C++
control flow.  The multiplier constructor theorem and the constant-multiplier
wrapper theorem are therefore strengthened with =Bar.bit_shift params = 0=.

** Add reciprocal admissibility to generic reciprocal theorems

The C++ API does not expose arbitrary =reciprocal= objects.  A reciprocal is
created by a constructor tied to a valid denominator in the parameter interval.
The Rocq model uses a plain record, so theorem statements over an arbitrary
=rec : reciprocal= must explicitly require that the stored denominator lies in
the range described by =reciprocal_params rec=.

Counterexample before strengthening:

- choose =params= with a large =min_denominator=, for example =2^128=
- choose a record =rec= whose stored =denominator_= is =1=
- set =reciprocal_= so that =reciprocal_invariant rec= holds

Then =max_quotient_bits params= is computed from the large minimum
denominator, while the actual denominator used by the theorem is =1=.  The
quotient storage chosen by =estimate_q= can be too small, so the generic
theorem may fail by truncating the quotient estimate.  The proof file therefore
introduces =reciprocal_admissible= and adds it to the generic
=estimate_q=, low-product, and =reduce= theorem statements.

The same predicate also records that =multiplier_= fits in
=MULTIPLIER_WORDS=.  This is enforced by the C++ fixed-size field and by the
constructor's debug assertion on the high words of =y=.  Without that bound,
an arbitrary Rocq record could store a much larger multiplier while the
parameter-derived =MAX_QUOTIENT_BITS= still assumes only =MULTIPLIER_BITS=
bits.

The predicate also records that the three stored word arrays have the
parameter-derived lengths used by the C++ object layout:
=reciprocal_words(params)=, =MAX_DENOMINATOR_WORDS=, and
=MULTIPLIER_WORDS=.  The Rocq record type itself permits arbitrary list
lengths, but the C++ fields are fixed-size =words_t<N>= members.  Without
these length facts, generic theorems over arbitrary Rocq reciprocals include
states that no C++ constructor can build.

Finally, the predicate records that the reciprocal scale factor is bounded by
=2^MULTIPLIER_BITS=.  This is automatic for the no-multiplier case and for the
exported constant-multiplier family with =MULTIPLIER_BITS = 256=.  It is also
the mathematical precondition used by =MAX_QUOTIENT_BITS=.  The generic C++
template does not fully enforce this when =MULTIPLIER_BITS= is not a multiple
of 64; that implementation issue is logged in
=barrett-semantic-discrepancies.org= and excluded from the generic quotient
theorems.

** Add proof-module dependencies to =BarrettProofs.MakeProofs=

The Barrett proofs rely on already-established facts about division, runtime
multiplication, and composite uint256 arithmetic.  The C++ semantics do not
change here; the proof functor is strengthened so it can use the existing
=DivisionProofs=, =RuntimeMulProofs=, and =ArithmeticProofs= results rather
than reproving those libraries locally.

** Exclude signed =INT_MIN / -1= overflow from =signed_wrapper_correct=

The signed wrapper theorem is strengthened with the usual two's-complement
overflow exclusion:

- =to_Z_signed_uint256 x = -sign_threshold256=
- =denominator_neg = true=
- =denominator_Z rec = 1=

Without this precondition, the mathematical quotient is =+sign_threshold256=,
which is not representable as a signed 256-bit value.  The Rocq model follows
the C++ control flow and returns the two's-complement word pattern
=0x8000...0000=; interpreted by =to_Z_signed_words=, that word pattern is
=-sign_threshold256=.

The strengthened theorem keeps the exact mathematical =Z.quot= and =Z.rem=
postcondition for every representable signed division case instead of proving
only a wrapped quotient.
